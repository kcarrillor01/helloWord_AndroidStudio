<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Fondo blanco -->
    <solid android:color="@color/white"/>

    <!-- Bordes -->
    <stroke
        android:width="2dp"
        android:color="@color/borders_color"/>

    <!-- Esquinas redondeadas -->
    <corners android:radius="12dp"/>
</shape>
